---
name: netease-mcmod-project
description: 在识别为网易 Minecraft Mod/MCMod 双包项目时，按项目目录隔离、代码注释、命名、清理和 manifest 校验规范创建或修改代码与资源；不适用于普通 Minecraft 项目或非网易包结构。
metadata:
  short-description: 网易 MCMod 项目规范
---

# 网易 MCMod 项目规范

当工作区符合以下任一组合时自动应用本 skill：存在行为包和资源包目录，且各自包含 `pack_manifest.json`；或路径/文件名包含 `netease_items_beh`、`netease_items_res`、`*_Scripts` 等网易 MCMod 结构。先扫描工作区确认真实目录，不要仅凭示例路径猜测。

## 项目识别与路径

- 从项目根目录和两个 pack 目录推导前缀、项目名、行为包目录、资源包目录；以现有目录和 manifest 名称为准。示例：前缀 `tnm`、项目名 `sf_subjoin`、版本目录 `tnm_sf_sub_814_9h`。
- 新建目录或文件前先检查父目录；缺失时创建完整目录树。
- 本项目作者名固定为 `qy`。新 Python 脚本放在 `<版本>/<项目>_beh/<项目>Scripts/<项目短名>/qy_<项目>/` 下；示例为 `.../tnm_sf_subjoinScripts/sfsubjoin/qy_sf_subjoin/`。
- 新 item 行为文件放在 `<版本>/<项目>_beh/netease_items_beh/qy_<项目>/` 下；对应资源文件遵循资源包现有结构和同样的作者/项目隔离规则。
- 对 entity、animation、UI、音效、模型等类型，按现有网易目录映射采用同样的隔离思路；若游戏系统要求固定目录，则遵守系统要求并记录例外。
- `materials`、`shaders`、`texts` 目录禁止增加作者/项目隔离层，必须直接放在系统识别的固定目录中。
- 修改已有文件时保持其现有位置；仅在新增文件或用户明确要求迁移时执行目录隔离。

## 代码格式

- 每个新增或修改的代码块（包括 Python、JSON 及其他文本格式）以空行分隔，并在块首、块尾写明详细中文注释，包含作者和月日标记。统一格式为 `# qyMMDD: 开始 - ...` 与 `# qyMMDD: 结束 - ...`；JSONC 等支持注释的格式使用对应合法语法。严格 JSON 不得插入注释或额外字段破坏 schema，改用同目录说明文件或任务报告记录首尾标记。日期使用本机当前日期的 `mmdd`，例如今天为 `qy0901`。注释应说明目的、输入/输出或副作用。
- 新增函数名和变量名使用驼峰式命名（`camelCase`）；类名保持项目既有风格。网易 API、引擎事件、序列化键、资源标识符和已有公共接口的原始大小写优先级高于本命名规则，禁止为了驼峰化而改名。
- 新增类的实例状态变量集中在 `__init__` 中，以 `self.xxx` 初始化；禁止新增类外大写常量或依赖隐式全局可变状态。必要的模块导入、引擎句柄和兼容性声明可位于类外。
- 优先复用项目已有基类、事件注册和配置模式，避免无关重构；新增代码应兼容 Python 运行时及网易 Mod API 版本。
- 保持 UTF-8 编码，避免引入无法被项目工具链识别的字符；中文注释可使用 UTF-8。

## 校验与收尾

- 每项任务结束时使用结构化 JSON 读取行为包和资源包的 `pack_manifest.json`，确认两者的 `format_version` 数值均为 `2`；缺失、非数字或不一致时修复并报告。不得递增 pack 版本号，不得修改任何 UUID。
- 检查新增 import、资源引用、命名空间、文件名大小写和相对路径，确保行为包/资源包引用一致。
- 删除本次任务产生的所有 `.pyc` 文件及 `__pycache__`/其他 pyc 文件夹；清理后再次扫描确认没有残留。不要删除用户明确保留的其他构建产物。
- 完成后报告新增/修改文件、实际落盘目录、manifest 检查结果和清理结果；若规则与网易引擎限制冲突，优先保证游戏可识别性并明确说明例外。

## 隔离目录外变更审批

开始修改前先计算本次任务所需的文件集合。若任何功能必需修改作者/项目隔离目录之外的文件（包括已有公共脚本、公共资源、根目录配置或其他项目目录），必须立即暂停实施并向用户说明文件路径、修改原因和影响，等待明确批准后才能继续；仅检查文件不需要批准。

## 规则边界

这些约束默认只影响本 skill 触发的网易 MCMod 项目任务。对已有代码的历史命名、类外状态或旧注释不做无关批量改写；只有新代码或为完成任务而触及的代码才按本规范处理。
